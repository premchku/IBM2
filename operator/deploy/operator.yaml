# Generated by IBM TransformationAdvisor
# Sat Jun 06 11:09:54 UTC 2020
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hostmanager-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: hostmanager-operator
  template:
    metadata:
      labels:
        name: hostmanager-operator
    spec:
      serviceAccountName: hostmanager-operator
      containers:
        - name: hostmanager-operator
          image: openliberty/operator:0.0.1
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "hostmanager-operator"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                - amd64
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 3
            preference:
              matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                - amd64
